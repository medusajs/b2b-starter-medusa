// Define link adapters for legacy entities when crossing module boundaries
// Example (replace with actual modules once implemented):
// import { defineLink } from "@medusajs/framework/utils";
// export default defineLink(LeftModule.linkable.entity, RightModule.linkable.entity);

