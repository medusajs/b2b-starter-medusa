MEDUSA_DEV_URL=http://localhost:8000
DATABASE_URL=postgres://<USER>:<PASS>@<HOST>:5432/<DB_NAME>

# HTTP
HOST=0.0.0.0
PORT=9000

# Segredos (defina localmente; não comitar válidos)
JWT_SECRET=<defina-um-segredo>
COOKIE_SECRET=<defina-um-segredo>

# (Opcional) Se usar admin em domínio separado:
# MEDUSA_BACKEND_URL=http://localhost:9000
# ADMIN_DISABLED=false

# (Opcional) Cache/Sessão:
# REDIS_URL=redis://<HOST>:6379
