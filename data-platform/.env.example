# ==========================================
# Data Platform — Environment Variables
# Copie para .env e preencha com valores reais
# ==========================================

# AWS/S3
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
S3_BUCKET=ysh-data-lake

# Pinecone (Vector Store)
PINECONE_API_KEY=pk_***
PINECONE_INDEX=ysh-rag
PINECONE_ENVIRONMENT=us-east-1-aws

# OpenAI (Embeddings & LLMs)
OPENAI_API_KEY=sk-***

# Medusa DB (já existe no docker-compose.yml principal)
# Apenas para referência; não é necessário redefinir
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_DB=medusa_db
# POSTGRES_USER=medusa_user
# POSTGRES_PASSWORD=medusa_password

# MinIO (S3-compatible local storage)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Dagster
DAGSTER_HOME=/opt/dagster/app
