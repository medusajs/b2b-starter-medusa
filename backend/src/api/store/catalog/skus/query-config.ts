
export const defaultStoreSkusFields = [
  "id",
  "created_at",
  "updated_at",
];

export const listSkusQueryConfig = {
  defaults: defaultStoreSkusFields,
  allowed: defaultStoreSkusFields,
  defaultLimit: 50,
};
