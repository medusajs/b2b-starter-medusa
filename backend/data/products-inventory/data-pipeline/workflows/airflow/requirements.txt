# Apache Airflow Requirements
# Python dependencies for YSH Data Pipeline DAGs

# Core Airflow
apache-airflow==2.8.0

# Airflow providers
apache-airflow-providers-amazon==8.15.0  # AWS integration
apache-airflow-providers-redis==3.5.0    # Redis backend
apache-airflow-providers-postgres==5.9.0 # PostgreSQL

# Pipeline dependencies
aiohttp>=3.9.0
asyncio>=3.4.3
feedparser>=6.0.10
redis>=5.0.0
psycopg2-binary>=2.9.9

# Web scraping (optional - if running scrapers in Airflow)
# crawlee>=0.3.0
# playwright>=1.40.0

# Ollama (optional)
# ollama>=0.1.0

# Utilities
python-dateutil>=2.8.2
python-dotenv>=1.0.0
requests>=2.31.0

# AWS SDK (for AWS deployment)
boto3>=1.34.0
botocore>=1.34.0

# Monitoring
prometheus-client>=0.19.0
